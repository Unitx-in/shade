Camera captures now work reliably — previously a race condition between state cleanup and result delivery caused every capture to crash before the image could be processed or returned to the app.
Files captured via camera can now be saved directly to external storage — configure a target path in the `saveToExternalStorage` block and Shade will write the final file there, removing the cache copy automatically.